{
  "name": "acme-cdk",
  "version": "1.0.0",
  "main": "dist/main.js",
  "types": "dist/main.ts",
  "license": "MPL-2.0",
  "private": true,
  "scripts": {
    "build:dev": "npm run build && NODE_ENV=development npm run synth",
    "build": "rm -rf dist && tsc",
    "synth": "cdktf synth",
    "compile": "tsc --pretty",
    "watch": "tsc -w",
    "test": "echo ok"
  },
  "engines": {
    "node": "=12"
  },
  "dependencies": {
    "@pocket-tools/terraform-modules": "^1.27.8"
  },
  "devDependencies": {
    "@types/node": "^12.20.27",
    "typescript": "4.4.3"
  }
}
